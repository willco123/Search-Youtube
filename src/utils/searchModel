const { ReadFileSyncByLine, ReadFileAsyncByLine } = require("./fileHelpers");

var searchParams = {
  part: "snippet",
  type: "video",
  maxResults: 50,
};

const searchArray = ReadFileSyncByLine("search_filter");

module.exports = {
  searchParams,
  searchArray,
};
