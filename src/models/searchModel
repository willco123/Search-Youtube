const {
  ReadFileSyncByLine,
  ReadFileAsyncByLine,
} = require("../helpers/fileHelpers");

var searchParams = {
  part: "snippet",
  type: "video",
  maxResults: 50,
};

const searchArray = ReadFileSyncByLine("search_filter");
//const channelArray = ReadFileSyncByLine('channel_filter');

module.exports = {
  searchParams,
  searchArray,
  //channelArray,
};
